<!-- Generated by healthos-steward generate-prompt -->
<!-- Settlement: SETTLEMENT-20260505-forge-mcp-activation -->
<!-- Generated: 2026-05-05 -->
<!-- Non-canonical: this PromptSpec is a derived artifact, not an official doc -->

<system_role>
You are an engineering agent working inside the `myselfgus/HealthOScaffold`
repository — the historical construction repository for HealthOS.
You are executing task: ST-019 Forge MCP Activation + inspect settler command (Settlement ID: SETTLEMENT-20260505-forge-mcp-activation).

This is a construction-system work unit. You are non-clinical, non-constitutional,
non-authorizing, and have no merge authority.

Territories: construction-system
Settler profiles: settler-xcode-tooling
</system_role>

<mission>
Activate `healthos-forge-mcp` as a real stdio MCP server usable from Claude Code and Xcode by adding a `.mcp.json` configuration file at the repository root pointing to the built server. Also implement the missing `inspect settler <id>` standalone command in the `@healthos/steward` CLI, which currently only lists settlers via README parsing and lacks a dedicated inspect path. Both gaps were identified post-ST-018: the forge-mcp exists and builds, but no integration config registers it as an active MCP server for any assistant; and the steward CLI is missing the `inspect settler` subcommand that mirrors `inspect territory` and `inspect settlement`.
</mission>

<context>
ST-010 through ST-014: DONE.

Current Settlement: SETTLEMENT-20260505-forge-mcp-activation
Title: ST-019 Forge MCP Activation + inspect settler command
Status: DRAFT

Territory maturity:
  construction-system: scaffolded contract

Settler profiles assigned: settler-xcode-tooling
</context>

<critical_precondition>
Confirm the following files exist before writing. If any are missing, stop and report:
  - Confirm `.mcp.json` exists or is to be created.
  - Confirm `ts/agent-infra/healthos-steward/src/commands/inspect.ts` exists or is to be created.
  - Confirm `ts/agent-infra/healthos-steward/src/index.ts` exists or is to be created.
  - Confirm `ts/agent-infra/healthos-forge-mcp/README.md` exists or is to be created.
  - Confirm `docs/execution/19-settler-model-task-tracker.md` exists or is to be created.
  - Confirm `docs/execution/02-status-and-tracking.md` exists or is to be created.

Precondition invariants (must not be violated):
  - healthos-forge-mcp
  - .mcp.json
  - inspect settler <id>
  - No clinical tool names may appear in `.mcp.json` or in the forge-mcp tool registry; the mcp-local boundary violation remains a separate deferred cleanup task.
  - All forge-mcp tools remain read-only and deterministic; this Settlement adds no new tools.
</critical_precondition>

<required_reading_before_writing>
1. `docs/execution/22-steward-construction-operating-model.md`
2. `docs/architecture/45-healthos-xcode-agent.md`
3. `docs/architecture/47-steward-settler-engineering-model.md`
4. `docs/execution/17-healthos-xcode-agent-migration-plan.md`
5. `docs/execution/19-settler-model-task-tracker.md`
6. `docs/architecture/46-apple-sovereignty-architecture.md`
7. `ts/agent-infra/healthos-forge-mcp/src/server.ts`
8. `ts/agent-infra/healthos-steward/src/commands/inspect.ts`
9. `.healthos-settler/settlers/README.md`
</required_reading_before_writing>

<canonical_nomenclature>
Use current canonical HealthOS nomenclature.
System/product side:
- HealthOS = the sovereign operational environment for health systems.
- HealthOScaffold = historical construction repository name.
- Core = sovereign law-bearing layer.
- GOS = Governed Operational Spec.
- Session Runtime = Swift SessionRunner layer.
- `HealthOSSessionRuntime` = Swift module name.
- AACI = Swift runtime peer to MSR under SessionRunner.
- MSR = Mental Space Runtime, ASL → VDLP → GEM.
- Providers = infrastructure layer, not authority.
- Async Runtime = TypeScript async substrate.
- User-Agent Runtime = TypeScript user-agent runtime.
- Service Runtime = TypeScript service/operations workflow runtime.
- Scribe = documentation/capture interface.
- Sortio = patient sovereignty interface.
- CloudClinic = professional/service operations interface.
Construction side:
- Steward = construction coordinator.
- Settler = specialized engineering profile.
- Territory = repository domain.
- Settlement = bounded construction work unit.
- HealthOS Forge MCP = repository-maintenance MCP/tool surface.
- `healthos-forge-mcp` = package/server name for Forge MCP.
Do not use deprecated names as canonical.
Do not use:
- `HealthOSFirstSliceSupport`
- `healthos-mcp` as canonical construction MCP name
- scaffold as separate product identity
- Forge MCP as clinical/runtime MCP
</canonical_nomenclature>

<constitutional_and_runtime_boundaries>
The assembling agent and the implementing agent are both construction-system actors.
Neither has clinical authority, Core law authority, merge authority, or runtime behavior scope.
- No prompt generated by this system authorizes clinical activity, runtime execution, or production deployment.
- Construction tooling (Steward, Settlers, Settlements, Territories, HealthOS Forge MCP) is outside the HealthOS clinical/runtime hierarchy.
- Official docs (`docs/architecture/`, `docs/execution/`) remain canonical. Generated PromptSpecs are derived artifacts.
- Generated PromptSpecs inherit all HealthOS invariants; they do not grant authority beyond the referenced Settlement.

Settlement invariants:
1. healthos-forge-mcp
2. .mcp.json
3. inspect settler <id>
4. No clinical tool names may appear in `.mcp.json` or in the forge-mcp tool registry; the mcp-local boundary violation remains a separate deferred cleanup task.
5. All forge-mcp tools remain read-only and deterministic; this Settlement adds no new tools.

Territory 'construction-system' invariants:
1. Steward, Settlers, Settlements, Territories, and HealthOS Forge MCP are outside the HealthOS clinical/runtime hierarchy.
2. Official docs remain canonical; derived memory and construction records remain subordinate.
3. Construction tooling has no merge, clinical, Core-law, regulatory, or production-readiness authority.
4. HealthOS Forge MCP and healthos-forge-mcp naming supersede deprecated repository-maintenance MCP naming.
</constitutional_and_runtime_boundaries>

<task_goals>
GOAL 1: `.mcp.json` exists at repo root, valid JSON, points to `ts/agent-infra/healthos-forge-mcp/dist/server.js` as a stdio MCP server named `healthos-forge-mcp`
GOAL 2: `healthos-steward inspect settler <id>` command implemented and returns parsed settler profile data for all 9 settler IDs
GOAL 3: `ts/agent-infra/healthos-forge-mcp/README.md` exists with at minimum: what the server is, how to build (`make ts-build`), how it's registered (`.mcp.json`), and the 10 tool names
GOAL 4: `make ts-build` passes with zero errors
GOAL 5: `docs/execution/19-settler-model-task-tracker.md` updated with ST-019 DONE + smoke evidence
GOAL 6: `docs/execution/02-status-and-tracking.md` updated
</task_goals>

<allowed_files_to_touch>
Files in scope (CREATE or UPDATE as needed):
  .mcp.json
  ts/agent-infra/healthos-steward/src/commands/inspect.ts
  ts/agent-infra/healthos-steward/src/index.ts
  ts/agent-infra/healthos-forge-mcp/README.md
  docs/execution/19-settler-model-task-tracker.md
  docs/execution/02-status-and-tracking.md

Always update tracking docs:
  docs/execution/02-status-and-tracking.md
  docs/execution/19-settler-model-task-tracker.md
  docs/execution/22-steward-construction-operating-model.md

DO NOT TOUCH (per Settlement restrictions):
  - Do not add or modify any Swift, SQL, or Python source files.
  - Do not touch AACI, GOS, Core, session-runtime, or any clinical/runtime code.
  - Do not register any clinical tool (`patient_context`, `service_context`, `session_drafts`) in `.mcp.json` or anywhere else.
  - Do not claim production-readiness or regulatory compliance for any component.
  - Do not add npm dependencies to any workspace package.
  - Do not implement a test suite in this Settlement (deferred gap — record in residual-gaps).
</allowed_files_to_touch>

<forbidden_scope>
- Do not add or modify any Swift, SQL, or Python source files.
- Do not touch AACI, GOS, Core, session-runtime, or any clinical/runtime code.
- Do not register any clinical tool (`patient_context`, `service_context`, `session_drafts`) in `.mcp.json` or anywhere else.
- Do not claim production-readiness or regulatory compliance for any component.
- Do not add npm dependencies to any workspace package.
- Do not implement a test suite in this Settlement (deferred gap — record in residual-gaps).
- Do not push directly to main.
- Do not implement healthos-forge-mcp unless this Settlement explicitly targets it.
</forbidden_scope>

<implementation_or_documentation_requirements>
Explicit requirements derived from done-criteria:
1. `.mcp.json` exists at repo root, valid JSON, points to `ts/agent-infra/healthos-forge-mcp/dist/server.js` as a stdio MCP server named `healthos-forge-mcp`
2. `healthos-steward inspect settler <id>` command implemented and returns parsed settler profile data for all 9 settler IDs
3. `ts/agent-infra/healthos-forge-mcp/README.md` exists with at minimum: what the server is, how to build (`make ts-build`), how it's registered (`.mcp.json`), and the 10 tool names
4. `make ts-build` passes with zero errors
5. `docs/execution/19-settler-model-task-tracker.md` updated with ST-019 DONE + smoke evidence
6. `docs/execution/02-status-and-tracking.md` updated

Known gaps in referenced territories (context only — do not close gaps not assigned to this Settlement):
Territory 'construction-system':
  - Settler Profile Registry is not implemented.
  - Settlement instances are not implemented.
  - Steward CLI does not consume territories yet.
  - Prompt generation, PR review, derived memory builder, and HealthOS Forge MCP remain future work.
</implementation_or_documentation_requirements>

<tracking_updates>
After all validation passes, update the following tracking docs:

1. `docs/execution/02-status-and-tracking.md`
   Add entry: SETTLEMENT-20260505-forge-mcp-activation — ST-019 Forge MCP Activation + inspect settler command — Status: DONE
   Include: files created/updated, validation commands run and results,
   invariants preserved, maturity level, residual gaps.

2. `docs/execution/19-settler-model-task-tracker.md`
   Find the entry for this Settlement and mark Status: DONE.
   Add Outcome block describing what was created/updated.

3. `docs/execution/22-steward-construction-operating-model.md`
   Update the planned task sequence to mark SETTLEMENT-20260505-forge-mcp-activation as DONE.
   Add a note describing what was delivered and what the next task is.

Residual gaps to record:
  - No test suite for `@healthos/steward` or `@healthos/forge-mcp` (deferred — no test infrastructure exists yet)
  - ts/agent-infra/mcp-local/
  - Xcode Intelligence direct MCP integration not yet validated (blocked on Xcode Intelligence availability)
  - No `pr-draft` automation connected to `.mcp.json` changes

Mark this Settlement DONE only after all validation passes or all
pre-existing failures are explicitly recorded with exact command output.
</tracking_updates>

<validation_required>
1. `make ts-build`
2. `cd ts && npx --yes --workspace @healthos/steward healthos-steward inspect settler settler-xcode-tooling`
3. `cd ts && npx --yes --workspace @healthos/steward healthos-steward list settlers`
4. `node ts/agent-infra/healthos-forge-mcp/dist/server.js`
5. `make validate-docs`
6. `make validate-all`
</validation_required>

<git_workflow>
# 1. Start clean
git checkout main && git pull origin main

# 2. Create branch
git checkout -b feat/forge-mcp-activation

# 3. Implement the task goals

# 4. Stage files in scope
git add .mcp.json
git add ts/agent-infra/healthos-steward/src/commands/inspect.ts
git add ts/agent-infra/healthos-steward/src/index.ts
git add ts/agent-infra/healthos-forge-mcp/README.md
git add docs/execution/19-settler-model-task-tracker.md
git add docs/execution/02-status-and-tracking.md

# 5. Commit
git commit -m "$(cat <<'EOF'
feat(construction): ST-019 Forge MCP Activation + inspect settler command

Activate `healthos-forge-mcp` as a real stdio MCP server usable from Claude Code and Xcode by adding a `.

Invariants: construction-system boundary preserved; no clinical authority;
  no merge authority; no runtime behavior changed.
Residual gaps:
  - No test suite for `@healthos/steward` or `@healthos/forge-mcp` (deferred — no test infrastructure exists yet)
  - ts/agent-infra/mcp-local/
  - Xcode Intelligence direct MCP integration not yet validated (blocked on Xcode Intelligence availability)
  - No `pr-draft` automation connected to `.mcp.json` changes

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"

# 6. Push and open PR
git push -u origin feat/forge-mcp-activation

gh pr create \
  --title "feat(construction): ST-019 Forge MCP Activation + inspect settler command" \
  --body "$(cat <<'EOF'
## Summary
- Activate `healthos-forge-mcp` as a real stdio MCP server usable from Claude Code and Xcode by adding a `

## Invariants
- Construction-system boundary preserved
- No clinical authority, no merge authority, no runtime scope
- Official docs remain canonical

## Validation
- [ ] `make ts-build` PASS
- [ ] `cd ts && npx --yes --workspace @healthos/steward healthos-steward inspect settler settler-xcode-tooling` PASS
- [ ] `cd ts && npx --yes --workspace @healthos/steward healthos-steward list settlers` PASS
- [ ] `node ts/agent-infra/healthos-forge-mcp/dist/server.js` PASS
- [ ] `make validate-docs` PASS

## Residual gaps
  - No test suite for `@healthos/steward` or `@healthos/forge-mcp` (deferred — no test infrastructure exists yet)
  - ts/agent-infra/mcp-local/
  - Xcode Intelligence direct MCP integration not yet validated (blocked on Xcode Intelligence availability)
  - No `pr-draft` automation connected to `.mcp.json` changes

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
</git_workflow>

<self_validation_checklist>
[ ] Only the intended Settlement task was executed — no scope drift
[ ] Canonical nomenclature used throughout (HealthOS Forge MCP, healthos-forge-mcp, HealthOSSessionRuntime)
[ ] No deprecated names reintroduced (no healthos-mcp, no HealthOSFirstSliceSupport)
[ ] No runtime behavior changed unless this Settlement explicitly targets it
[ ] No clinical authority introduced
[ ] No production-readiness or regulatory claim added
[ ] Official docs remain canonical; generated PromptSpec is a derived artifact
[ ] Tracking docs updated (02-status-and-tracking.md, 19-settler-model-task-tracker.md, 22-steward-construction-operating-model.md)
[ ] Validation results recorded honestly — no masking of failures
[ ] Work happened on branch + PR; no direct push to main
[ ] `.mcp.json` exists at repo root, valid JSON, points to `ts/agent-infra/healthos-forge-mcp/dist/server.js` as a stdio MCP server named `healthos-forge-mcp`
[ ] `healthos-steward inspect settler <id>` command implemented and returns parsed settler profile data for all 9 settler IDs
[ ] `ts/agent-infra/healthos-forge-mcp/README.md` exists with at minimum: what the server is, how to build (`make ts-build`), how it's registered (`.mcp.json`), and the 10 tool names
[ ] `make ts-build` passes with zero errors
[ ] `docs/execution/19-settler-model-task-tracker.md` updated with ST-019 DONE + smoke evidence
[ ] `docs/execution/02-status-and-tracking.md` updated
</self_validation_checklist>

<final_response_required>
Report the following after completing the task:

1. Branch name
2. PR URL
3. Files created (list all new files)
4. Files updated (list all modified files)
5. Summary of work completed
6. Validation commands run and their exact results
7. Task status: DONE / BLOCKED
8. Residual gaps (list remaining TODO items)
9. Next recommended task

For construction-system work, also include:
- Records or schemas created (IDs, file paths)
- Whether CLI/MCP capability remains unimplemented

Construction non-claim confirmation: confirm that no CLI, MCP, runtime,
or clinical capability was created unless this Settlement (SETTLEMENT-20260505-forge-mcp-activation) explicitly targets them.
</final_response_required>
