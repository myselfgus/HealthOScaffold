# Derived Steward Memory

This directory is for deterministic outputs from `healthos-steward build-memory` and `steward_build_memory`.

Derived memory is non-canonical. It accelerates navigation and handoff, but official docs, source files, schemas, and reviewed Settlement records remain the evidence source.

Generated snapshot files in this directory are intentionally ignored by git. Regenerate them locally when needed.
