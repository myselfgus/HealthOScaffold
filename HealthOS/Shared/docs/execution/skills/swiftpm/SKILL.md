# Skill: SwiftPM

## Overview
Use this skill when `Package.swift` is the primary entrypoint or SwiftPM is the fastest path to a result.

## Workflow
1. Inspect `Package.swift`.
2. Build with `swift build`.
3. Run with `swift run <product>`.
4. Test with `swift test`.
