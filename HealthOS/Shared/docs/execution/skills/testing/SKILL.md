# Skill: Testing

## Overview
Use this skill to run the smallest meaningful test scope first, classify failures precisely, and avoid treating every failure like a product bug.

## Workflow
1. Detect harness (Xcode vs SwiftPM).
2. Narrow scope (product/test filter).
3. Classify (Build, Assertion, Crash, Async, Env).
4. Summarize failure clearly.
