# Python ML boundary

Python is reserved for offline ML operations:
- dataset preparation
- evaluation
- adapter/LoRA job scaffolding
- registry and promotion logic

It is intentionally not the primary online runtime.
