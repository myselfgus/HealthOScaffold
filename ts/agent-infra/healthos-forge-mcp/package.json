{
  "name": "@healthos/forge-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HealthOS Forge MCP — repository-maintenance MCP server for Steward",
  "main": "dist/server.js",
  "bin": {
    "healthos-forge-mcp": "dist/server.js",
    "healthos-forge-mcp-http": "dist/server-http.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/**/*.test.mjs",
    "start:http": "node dist/server-http.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@healthos/steward": "0.2.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
