{
  "name": "@healthos/managed-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HealthOS Steward Coordinator Managed Agent — construction-system only, no clinical authority",
  "main": "dist/index.js",
  "bin": {
    "healthos-create-agent": "dist/create-agent.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "create-agent": "node dist/create-agent.js",
    "create-agent:dry-run": "node dist/create-agent.js --dry-run",
    "create-agent:force": "node dist/create-agent.js --force"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
